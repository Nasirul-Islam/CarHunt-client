import { Container } from '@mui/material';
import React from 'react';

const Footer = () => {
    return (
        <Container>
            <h2>Footer</h2>
        </Container>
    );
};

export default Footer;